@page "/"

@inject ICategoryData categoryData
@inject IStatusData statusData
@inject ISuggestionData suggestionData
@inject IUserData userData
@inject NavigationManager navManager /* navigate between pages */
@inject ProtectedSessionStorage seesionStorage



@code {
    private List<BasicSuggestionModel> suggestions;
    private List<CategoryModel> categories;
    private List<StatusModel> statuses;

    private string selectedCatory = "All";
    private string selectedStatus = "All";
    private string searchText = "";
    bool isSortedByNew = true;

    protected async override Task OnInitializedAsync()
    {
        categories = await categoryData.GetAllCategories();
        statuses = await statusData.GetAllStatuses();
    }

    protected async override Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await LoadFilterState(firstRender);
            await FilterSuggestions();
            StateHasChanged();
        }


    }

    private async Task  LoadFilterState()
    {
        
    }

    // Stop at 16:53
    // Link: https://www.youtube.com/watch?v=B-ETrXaAXlE&list=PLLWMQd6PeGY0cZFMqx5ijmdaD87sJKCsU&index=14

}




 